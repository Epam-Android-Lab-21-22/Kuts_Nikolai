<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/customer_item_background"
    android:layout_margin="@dimen/item_margin"
    android:padding="@dimen/item_padding">

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/customer_name"
        style="@style/text_view"
        tools:text="name"
        android:textSize="@dimen/name_text_size"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/customer_id"
        style="@style/text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="id"
        android:textSize="@dimen/customer_id_text_size"
        android:paddingStart="@dimen/text_view_padding"
        app:layout_constraintStart_toEndOf="@id/customer_name"
        app:layout_constraintBaseline_toBaselineOf="@id/customer_name"
        tools:ignore="RtlSymmetry" />


    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/customer_account"
        style="@style/text_view"
        tools:text="account"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/customer_name"
        />


</androidx.constraintlayout.widget.ConstraintLayout>
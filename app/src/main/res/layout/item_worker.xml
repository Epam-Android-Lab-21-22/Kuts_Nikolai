<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/worker_item_background"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/item_margin"
    android:padding="@dimen/item_padding">

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/worker_first_name"
        style="@style/text_view"
        tools:text="name"
        android:textSize="@dimen/name_text_size"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/worker_last_name"
        style="@style/text_view"
        tools:text="country"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/worker_first_name"
        />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/worker_company"
        style="@style/text_view"
        tools:text="city"
        android:paddingStart="@dimen/text_view_padding"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@id/worker_last_name"
        app:layout_constraintTop_toBottomOf="@id/worker_first_name"
        tools:ignore="RtlSymmetry" />

</androidx.constraintlayout.widget.ConstraintLayout>